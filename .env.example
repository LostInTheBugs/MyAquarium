# MyAquarium — docker compose configuration
# Hostname the reverse proxy (Traefik) routes to this app.
# Used by docker-compose.yml (${DOMAIN:-your-domain.example.com}).
DOMAIN=your-domain.example.com

# Internal listen port of the nginx container (overridable).
PORT=8000
